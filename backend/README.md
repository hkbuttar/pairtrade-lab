# backend/

Optional thin FastAPI layer over `pairs/`, `signals/`, `monitor/`, and
`backtest/` results, for programmatic access beyond the Panel app. Not a hard
requirement since Panel serves its own frontend.

Not yet implemented.
